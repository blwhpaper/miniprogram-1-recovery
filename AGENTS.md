# AGENTS.md

## 0. 适用范围与总原则（强制执行）

本文件是本项目唯一执行规则真源，用于约束本项目内所有任务执行与输出格式。

强制原则：

- 先读代码，再改代码
- 优先复用现有实现
- 按最小改动实现
- 不擅自扩大范围
- 不顺手重构或清理无关代码
- 只修改当前任务相关文件
- 不把“已改本地”表述成“已经线上生效”
- 不混淆“事实检查 / 开发实现 / 测试验证 / 收工输出 / 任务包 / Codex 提示词”六类模式
- 结论必须直接，不允许含糊
- 输出必须可复制、可执行、可验收
- 若代码事实与之前口头判断冲突，必须以代码事实为准
- 本项目只认 AGENTS.md，不再维护或引用 AGENTS.dev.md

---

## 1. 模式识别与优先级（强制执行）

用户请求默认按以下模式识别：

1. 收工模式
2. 测试分身模式
3. 事实检查模式
4. Codex 提示词模式
5. 任务包模式
6. 开发实现模式

若一个请求同时涉及多种模式，优先级按上面顺序执行。

若用户当前消息意图已经非常明确，以当前消息直接意图为准，不混入其他模式内容。

---

## 2. 收工模式（强制执行）

当用户输入以下任一类话术时，进入【收工模式】：

- 收工
- 收工 TASK-xxxx 中文任务名
- 语义等价的明确收工指令

### 2.1 收工执行规则

必须执行：

1. 强制调用 wechat-task-closeout skill
2. 不允许模型自行模拟收工内容
3. 输出必须来自 skill，包括：
   - Git 提交命令
   - Obsidian 笔记内容或对应输出文件结果

只有 skill 失败时，才允许 fallback。

### 2.2 收工输出范围

收工时只允许输出两部分，且顺序固定：

## 1. Git 代码
git add <真实修改文件1> <真实修改文件2> ... && git commit -m "docs: 更新 TASK-xxxx 中文任务名"

## 2. Obsidian 笔记
（完整笔记内容）

### 2.3 收工禁止事项

禁止：

- 禁止输出第 3 部分
- 禁止追加解释说明
- 禁止追加开发分析
- 禁止追加测试建议
- 禁止追加“后续建议”
- 禁止追加“下一个任务启动包”
- 禁止追加 Codex 提示词
- 禁止输出 git status 代替收工结果
- 禁止输出需要用户自己拼接的命令片段
- 禁止把收工结果和其他模式内容混在一起

### 2.4 收工 Git 规则

收工时输出的 Git 命令必须满足以下全部要求：

- git add 和 git commit 必须合并为一整条单行命令
- 不允许拆成两条命令分别输出
- 不允许输出 git status
- 不允许只输出 commit message
- 不允许省略 git add
- 不允许省略 git commit
- 禁止使用 `git add .`
- 禁止使用 `git add -A`
- 禁止 add 整个目录代替具体文件
- 只 add 本任务实际修改文件
- 禁止遗漏本任务真实修改文件
- Git 命令必须放在单独代码块中
- 代码块中只允许这一条单行命令
- 即使界面视觉折行，输出源文本也必须保持单行

固定格式为：

git add <真实修改文件1> <真实修改文件2> ... && git commit -m "docs: 更新 TASK-xxxx 中文任务名"

### 2.5 收工 commit message 规则

- commit message 必须固定为中文简洁风格
- 固定格式为：

docs: 更新 TASK-xxxx 中文任务名

- 任务编号必须保持用户给定写法
- 不允许擅自改 TASK 编号大小写
- 不允许擅自改任务中文名
- 不允许省略 TASK 编号
- 不允许写成与任务不一致的描述
- 不允许使用“我已经改了”这类口语化提交信息

### 2.6 收工合格示例

合格示例：

git add miniprogram/pages/teacherHome/teacherHome.js miniprogram/pages/teacherHome/teacherHome.wxml && git commit -m "docs: 更新 TASK-018F6C teacherHome 按钮显示口径收口"

不合格示例：

- git add 被拆成两行
- 路径中间断行
- git add 和 git commit 分开输出
- commit message 被换到下一行
- 只输出 git status
- 使用 `git add .`
- commit message 缺少 TASK 编号
- commit message 写成“我已经改了 ……”

### 2.7 收工 fallback 规则

只有在 wechat-task-closeout skill 不可用时，才允许模型自行生成收工内容。

即使 fallback，也必须满足：

- 输出结构完全一致
- 仍然只输出两部分
- Git 命令仍严格遵守单行规则
- commit message 仍严格遵守固定格式
- 不得因为 fallback 就放宽格式要求

---

## 3. 任务包模式（强制执行）

当用户输入以下任一类话术时，进入【任务包模式】：

- 生成任务包
- 下一个任务包
- 给我下个任务启动包
- 给我新窗口任务包
- 任务开启包
- 启动包

### 3.1 任务包输出范围

只允许输出四项：

- 任务名
- 任务目标
- 本轮边界
- 预期结果

### 3.2 任务包强制要求

- 不写代码
- 不写实现步骤
- 不写具体修改方案
- 不扩展需求
- 不追加 Codex 提示词
- 不混入收工内容
- 不混入 Git / Obsidian 内容
- 不混入测试清单
- 不擅自扩大任务边界
- 优先复用用户已给出的任务口径
- 模板应简洁，可直接复制到新窗口

### 3.3 任务包禁止追加项

除非用户明确要求，否则不要追加：

- 文件列表
- 风险分析
- 验收清单
- 部署提醒
- 开发建议

---

## 4. Codex 提示词模式（强制执行）

当用户明确要求以下任一类话术时，进入【Codex 提示词模式】：

- 输出 codex 执行提示词
- 给我 codex 提示词
- 发我 codex 提示词
- 给我一段直接发给 codex 的内容

### 4.1 Codex 提示词输出要求

- 只输出可直接发给 Codex 的完整文本
- 提示词必须明确写明：
  - 任务名
  - 任务目标
  - 本轮边界
  - 预期结果
  - 执行要求
  - 读取文件范围
  - 输出结构
- 必须强调：
  - 先读代码，再改代码
  - 优先复用现有实现
  - 按最小改动实现
  - 不擅自扩大范围
  - 不顺手重构

### 4.2 Codex 提示词禁止事项

- 不混入收工输出
- 不混入“是否继续”
- 不写多版本备选
- 不给模糊建议
- 不把任务包和 Codex 提示词混在一起，除非用户明确要求

---

## 5. 开发实现模式（强制执行）

当任务是以下任一类时，进入【开发实现模式】：

- 实现功能
- 修复问题
- 收口逻辑
- 调整显示口径
- 补齐门禁
- 优化已有链路

### 5.1 开发实现强制要求

必须：

- 先读代码，再改代码
- 优先复用现有实现
- 按最小改动实现
- 不擅自扩大范围
- 不顺手重构或清理无关代码
- 只修改当前任务相关文件
- 不新增无必要状态体系
- 不改无关 UI
- 不改无关页面
- 不改用户未授权的业务链路
- 不把推测写成事实

### 5.2 开发实现输出结构（固定）

输出必须为：

1. 读取并分析了哪些文件
2. 修改了哪些文件
3. 每个文件具体改了什么
4. 当前问题是否已解决
5. 当前链路数据挂载位置
6. 当前这版做到什么、没做什么
7. 部署提醒（若涉及云函数则必填）

### 5.3 开发实现补充要求

- 必须分文件说明修改内容
- 必须说明本轮边界内做到什么
- 必须明确说没做什么
- 若存在数据真源，必须明确指出
- 若存在门禁 / 分发 / 状态口径，必须明确指出真实依据
- 若改动涉及云函数，必须提醒重新上传并部署

---

## 6. 事实检查模式（强制执行）

当用户要求以下任一类内容时，进入【事实检查模式】：

- 看看现在是不是这样
- 查一下真实链路
- 检查集合名
- 读取代码判断
- 不要改代码，只检查
- 帮我确认真实口径

### 6.1 事实检查规则

- 不改代码
- 不创建数据
- 不补功能
- 不顺手修 bug
- 不把推测写成事实
- 必须以真实代码、真实字段、真实调用链为准
- 若发现之前口径与代码不一致，必须直接纠正

### 6.2 事实检查输出结构（固定）

输出必须为：

1. 读取并检索了哪些文件
2. 实际检索到的关键对象清单
3. 当前真实链路结论
4. 当前配置 / 环境 / 数据挂载结论
5. 问题成因或差异原因
6. 最小行动建议

---

## 7. 测试分身模式（强制执行）

当用户说以下任一类话术时，进入【测试分身模式】：

- 做测试
- 验证
- debug
- 检查这版是否有问题
- 给我验收清单
- 给我手测步骤
- 回归检查一下

### 7.1 测试分身规则

- 不改代码
- 不扩需求
- 不重构
- 只做验证与回归检查
- 场景必须覆盖用户当前任务边界
- 预期结果必须与真实口径一致

### 7.2 测试分身输出结构（固定）

输出必须为：

1. 本次测试目标
2. 核心场景清单
3. 每个场景操作步骤
4. 预期结果
5. 风险点
6. 回归检查点

---

## 8. 状态语义规则（必须遵守）

### 8.1 处理态

- 待处理
- 已确认
- 已关闭

### 8.2 生效态

- 已签到
- 已请假
- 旷课
- 未生效

### 8.3 动作

- 设为请假
- 设为旷课
- 确认申请

### 8.4 强制要求

- 不要把申请状态和结果状态写在一句话里
- 按钮文案必须是动作，不要写成状态
- 页面展示状态必须与数据状态一致
- toast 文案必须与用户实际操作一致
- 历史课次与当前课次语义不能混用

---

## 9. 重点检查页面（优先关注）

优先关注：

- signRecord
- studentSign
- studentHome
- classInteraction
- randomRollcall

检查重点：

- 按钮文案是否是动作
- 状态展示是否和数据一致
- toast 是否与操作一致
- 当前课次 vs 历史课次边界是否正确
- 页面门禁是否与身份状态一致
- 回跳落点是否与首页分发逻辑一致

---

## 10. 云函数规则（强制执行）

如果本轮改动涉及以下任一内容，则最终汇报必须追加部署提醒：

- cloudfunctions/ 下新增云函数
- cloudfunctions/ 下修改 index.js
- cloudfunctions/ 下修改 package.json
- 前端新增或修改 `wx.cloud.callFunction`
- 前端新增或修改云函数返回值判断逻辑

### 10.1 部署提醒固定结构

### 7. 部署提醒

- 本轮涉及的云函数：
- 是否需要重新上传并部署：
- 建议部署方式：微信开发者工具 → 右键云函数目录 → 上传并部署：云端安装依赖

### 10.2 云函数提醒强制要求

必须明确写出：

- 改了云函数，不等于云端已生效
- 不允许默认假设用户已经部署
- 若未部署，可能出现：
  - errCode: -501000 | FunctionName parameter could not be found
  - 云端仍执行旧逻辑
  - 前端和云端返回结构不一致

### 10.3 云函数补充规则

- 只要改了云函数，必须提醒部署
- 不允许省略部署提醒
- 若本轮未涉及云函数，也要明确写：
  - 本轮不涉及云函数部署

---

## 11. 云函数检查项（强制执行）

涉及云函数时，必须顺手检查：

- 云函数目录名与 `callFunction({ name })` 是否完全一致
- 是否新增了未部署云函数
- package.json 是否需要一起部署
- 返回值结构改了，前端是否同步修改判断逻辑
- 是否可能连到了旧云环境 / 旧版本函数
- 云函数依赖的 collection / 字段名是否与前端口径一致

---

## 12. 最终汇报口径（强制执行）

结论必须直接，不允许含糊。

正确写法：

- 本轮涉及云函数：createLesson、createSignCode
- 需要重新上传并部署：是

错误写法：

- 可能需要部署
- 建议视情况部署
- 如果没问题可以不部署

### 12.1 结论表达要求

- 必须区分“已经改本地”和“已经云端生效”
- 必须区分“当前做到的”和“本轮没做的”
- 必须区分“真实代码事实”和“建议动作”
- 不允许把猜测写成既成事实

---

## 13. 角色与权限口径规则（强制执行）

当任务涉及以下任一模块时：

- studentHome
- teacherHome
- classManager
- adminTeacherReview
- 首页分发
- 教师资格显示
- 教师态退出 / 恢复

必须遵守：

- 教师资格是否生效，优先看真实资格字段，不看模糊文案
- 页面显示按钮必须互斥，不允许冲突并存
- “已通过退出态”与“未申请态”不能落到同一按钮分支
- 若同一账号同时具备学生基础身份与教师资格，必须明确谁是当前权限真源
- 不擅自引入多角色系统，除非用户明确要求
- 不擅自改角色字段语义，除非用户明确要求

---

## 14. 数据真源表达规则（强制执行）

当任务涉及数据状态、集合名、字段名、页面判断口径时，必须：

- 区分集合名和字段名
- 区分本地缓存态和远端真源态
- 区分显示文案和真实状态
- 若用户把字段名误认成集合名，必须直接纠正
- 若代码与之前口头分析不一致，必须以代码事实为准修正结论

---

## 15. 禁止事项（总表）

禁止：

- 禁止把收工、任务包、开发汇报、测试清单、Codex 提示词混在一起
- 禁止在未读代码前直接下实现结论
- 禁止用猜测替代代码事实
- 禁止顺手重构无关代码
- 禁止擅自扩大需求
- 禁止改了云函数却不提醒部署
- 禁止把云函数未部署误判成前端问题
- 禁止把“本地已改”写成“已经生效”
- 禁止漏写需要部署的云函数名称
- 禁止把字段名说成集合名
- 禁止把页面文案当作权限真源
- 禁止输出需要用户自行拼接的 Git 多行命令
- 禁止输出 `git add .`
- 禁止在收工时输出被拆断的路径
- 禁止让 AGENTS.dev.md 重新作为规则源出现

---

## 16. Git 输出补充约束（用于彻底避免“看起来被截断”争议）

为避免 Git 命令在展示时产生“像被截断”的歧义，强制执行以下附加规则：

- Git 命令必须始终放在独立代码块中
- 代码块中只允许出现 1 行
- 该 1 行中不允许出现真实换行
- 不允许把双引号中的 commit message 放到下一行
- 不允许输出任何需要用户自己拼接的断行片段
- 用户应可直接整行复制执行
- 若界面出现视觉折行，视为显示问题；模型输出源文本仍必须保持单行
- 模型不得以“界面会自动折行”为由放宽单行约束

---

## 17. 本文件覆盖原则

规则冲突时，按以下优先级执行：

1. 收工输出结构与 Git 单行规则
2. 云函数部署提醒规则
3. 模式隔离规则
4. 最小改动开发规则
5. 其他补充规则

若用户在当前对话中给出更具体、更明确的新规则，且不与以上强制规则冲突，应优先遵守用户当前规则。

---

## 18. 单一规则源原则（强制执行）

本项目规则文件只保留：

- AGENTS.md

明确废弃：

- AGENTS.dev.md

要求：

- 不再创建 AGENTS.dev.md
- 不再引用 AGENTS.dev.md
- 不再从任何模板、脚本、skill、提示词中输出 AGENTS.dev.md
- 若仓库中再次出现 AGENTS.dev.md，应视为旧模板或外部脚本回灌，优先删除并排查生成来源